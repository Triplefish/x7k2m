[build]
builder = "NIXPACKS"

[deploy]
startCommand = "flask db upgrade && gunicorn app:app --workers 2 --bind 0.0.0.0:$PORT --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
